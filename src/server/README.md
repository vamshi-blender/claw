# Server Foundation

Future backend orchestration code belongs here.

The current setup intentionally does not include OpenAI SDK agent logic, tool
calling loops, session management, authentication, database access, or streaming
agent runs.
